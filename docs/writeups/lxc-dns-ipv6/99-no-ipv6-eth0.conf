# /etc/sysctl.d/99-no-ipv6-eth0.conf — deployed in every LXC on thearchivist (2026-09-18)
# Archivist segment has no IPv6 route; drop link-local so glibc AI_ADDRCONFIG suppresses AAAA.
# eth0 only: keeps ::1 on loopback and leaves wg0 (CT 113) untouched.
net.ipv6.conf.eth0.disable_ipv6 = 1
