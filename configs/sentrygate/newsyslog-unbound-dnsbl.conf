# Rotate the DNSBL bridge log so it cannot fill /var on a 3 GB box with no swap.
# Install to /etc/newsyslog.conf.d/unbound-dnsbl.conf
# logfilename                        [owner:group]  mode count size when  flags
/var/log/unbound-dnsbl/blocks.log                   644  7     1000 *     CJ
